# Pre-launch: block all crawlers while site is in review.
# At launch (when LIVE flag flips to true in site.config.ts),
# replace this file with the production version that allows crawling
# and re-introduces the Sitemap reference pointing to the .com.uy domain.

User-agent: *
Disallow: /
